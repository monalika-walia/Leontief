{
  "name": "@leontief/indexer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Polls Soroban RPC getEvents for the Leontief contracts → Postgres + REST",
  "scripts": {
    "migrate": "tsx src/migrate.ts",
    "poll": "tsx src/poller.ts",
    "api": "tsx src/api.ts",
    "dev": "tsx src/index.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^16.2.0",
    "@fastify/cors": "^11.0.0",
    "fastify": "^5.2.0",
    "zod": "^3.24.0",
    "postgres": "^3.4.5",
    "tsx": "^4.20.0"
  },
  "devDependencies": {
    "vitest": "^3.2.0"
  }
}
