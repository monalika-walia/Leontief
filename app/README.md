# app/

React dApp (Phase A2): wallet modal via @creit.tech/stellar-wallets-kit, optional passkey
smart-wallet path, reads via simulation, writes with pre-flight simulation and typed-error
mapping. Consumes @leontief/sdk only — no raw bindings imports.
