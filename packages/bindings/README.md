# packages/bindings/

Generated TypeScript clients (`stellar contract bindings typescript`) — one directory per
contract, produced by `just bindings` from `deployments/testnet.json` (Phase A1).
Generated output is gitignored; regenerate after every deploy.
